import palavras from "./palavras"
function comecarJogo(){
    let palavraSelecionada = palavras[Math.floor(Math.random() * palavras.length)]

    // se a variavel com a palavra for diferente de null libera o teclado ativado

    // se a variavel com a palavra for diferente de null liberar o imput
    
    //mostras uma variavel q contem a palavra porem apenas o numero de caracteres
    
}
export default comecarJogo